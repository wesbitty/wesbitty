import Code from './assets/code-brackets.svg'
import Colors from './assets/colors.svg'
import Comments from './assets/comments.svg'
import Direction from './assets/direction.svg'
import Flow from './assets/flow.svg'
import Plugin from './assets/plugin.svg'
import Repo from './assets/repo.svg'
import StackAlt from './assets/stackalt.svg'
import { Meta } from '@storybook/addon-docs'
import './Introduction.css'

<Meta title="Wesbitty UI" />

<style>
  {`
  .firstheading {
    --mediumdark: '#999999';
    font-weight: 900;
    font-size: 20px;
    color: #000;
    letter-spacing: 3px;
    line-height: 24px;
    text-transform: uppercase;
    margin-bottom: 20px;
  }
    .heading {
    --mediumdark: '#999999';
    font-weight: 900;
    font-size: 13px;
    color: #000;
    letter-spacing: 2px;
    line-height: 24px;
    margin-bottom: 20px;
    margin-top: 20px;
  }
  .subheading {
    --mediumdark: '#999999';
    font-weight: 900;
    font-size: 13px;
    color: #000;
    letter-spacing: 2px;
    line-height: 24px;
    text-transform: uppercase;
    margin-bottom: 4px;
    margin-top: 40px;
  }

  .link-list {
    display: grid;
    grid-template-columns: 1fr;
    grid-template-rows: 1fr 1fr;
    row-gap: 10px;
  }

  @media (min-width: 620px) {
    .link-list {
      row-gap: 20px;
      column-gap: 20px;
      grid-template-columns: 1fr 1fr;
    }
  }

  @media all and (-ms-high-contrast:none) {
  .link-list {
      display: -ms-grid;
      -ms-grid-columns: 1fr 1fr;
      -ms-grid-rows: 1fr 1fr;
    }
  }

  .link-item {
    display: block;
    padding: 20px 30px 20px 15px;
    border: 1px solid #00000010;
    border-radius: 5px;
    transition: background 150ms ease-out, border 150ms ease-out, transform 150ms ease-out;
    color: #333333;
    display: flex;
    align-items: flex-start;
  }

  .link-item:hover {
    border-color: #1EA7FD50;
    transform: translate3d(0, -3px, 0);
    box-shadow: rgba(0, 0, 0, 0.08) 0 3px 10px 0;
  }

  .link-item:active {
    border-color: #1EA7FD;
    transform: translate3d(0, 0, 0);
  }

  .link-item strong {
    font-weight: 700;
    display: block;
    margin-bottom: 2px;
  }
  
  .link-item img {
    height: 40px;
    width: 40px;
    margin-right: 15px;
    flex: none;
  }

  .link-item span {
    font-size: 14px;
    line-height: 20px;
  }

  .tip {
    display: inline-block;
    border-radius: 1em;
    font-size: 11px;
    line-height: 12px;
    font-weight: 700;
    background: #E7FDD8;
    color: #66BF3C;
    padding: 4px 12px;
    margin-right: 10px;
    vertical-align: top;
  }

  .tip-wrapper {
    font-size: 13px;
    line-height: 20px;
    margin-top: 40px;
    margin-bottom: 40px;
  }

  .tip-wrapper code {
    font-size: 12px;
    display: inline-block;
  }

  
  `}
</style>

<div class="firstheading">Wesbitty UI</div>{' '}

<div class="heading">Wesbitty UI is a React UI library.</div>

🚧 **Note:** Wesbitty UI is still a work-in-progress until `ace` release is published.

<div class="subheading">Installation</div>

```cli
npm install ui
```

<div class="subheading">Configure</div>

**Update `tailwind.config.js` to the following**

You will need to wrap the WesbittyUI config around your own tailwind config like below.
Also add the new purge rules

```js
// tailwind.config.js
const { wesjetTheme } = require('ui')

module.exports = wesjetTheme({
  purge: [
    './pages/**/*.{js,ts,jsx,tsx}',
    './components/**/*.{js,ts,jsx,tsx}',
    './node_modules/ui/types/theme/default.js', // add this
  ],
  theme: {
    extend: {},
  },
  plugins: [],
})
```

<div class="subheading">Usage</div>

- Button

```js
import { Button } from 'ui'

//...

return <Button>Wesbitty UI button</Button>
```

- Icons

We use [Feather icon library](https://feathericons.com/) in Wesbitty UI

You can use any Icon from the library as a component by prepending `Icon` to any Icon name, like, `<IconMail>`

```js
import { IconMail } from 'ui'

//...

return <IconMail size="small" />
```

<div class="subheading">Learn</div>

<div class="link-list">
  <a class="link-item" href="https://github.com/wesbitty/ui" target="_blank">
    <img src={Repo} alt="repo" />
    <span>
      <strong>Wesbitty UI documentation</strong>
      Configure, customize, and extend
    </span>
  </a>
  <a class="link-item" href="https://github.com/wesbitty/ui" target="_blank">
    <img src={Direction} alt="direction" />
    <span>
      <strong>In-depth guides</strong>
      Best practices from leading teams
    </span>
  </a>
  <a class="link-item" href="https://github.com/wesbitty/ui" target="_blank">
    <img src={Code} alt="code" />
    <span>
      <strong>GitHub project</strong>
      View the source and add issues
    </span>
  </a>
  <a class="link-item" href="https://discord.gg/UUt2PJb" target="_blank">
    <img src={Comments} alt="comments" />
    <span>
      <strong>Discord chat</strong>
      Chat with maintainers and the community
    </span>
  </a>
</div>
