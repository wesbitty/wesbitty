import React from 'react';
type Props = {
    children: React.ReactNode;
    active: boolean;
};
export default function Loading({ children, active }: Props): import("react/jsx-runtime").JSX.Element;
export {};
