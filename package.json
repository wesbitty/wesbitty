{
  "name": "wesbitty",
  "version": "0.0.0",
  "private": true,
  "description": "We've Got Your WishList",
  "keywords": [
    "wesbitty",
    "platform",
    "Ecommerce",
    "application",
    "hosting"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wesbitty/wesbitty.git"
  },
  "license": "Apache-2.0",
  "author": "Wesbitty, Inc.",
  "workspaces": [
    "packages/*",
    "apps/*",
    "examples/*",
    "tests"
  ],
  "scripts": {
    "prepare": "husky install",
    "build": "turbo run build --filter=ui --filter=./examples/*",
    "build:app": "turbo run build --filter=web",
    "dev": "turbo run dev --parrallel --continue --filter=web...",
    "lint": "eslint . --ext .ts --fix",
    "lint-package": "sort-package-json",
    "format": "prettier --write .",
    "clean": "turbo run clean --no-cache",
    "prisma-generate": "cd ./apps/web && npx prisma generate",
    "pscale-up": "cd apps/web && pscale connect wesbitty main",
    "test": "turbo run turbo run test --concurrency=1 --filter=[HEAD^1] --filter=./packages/*"
  },
  "lint-staged": {
    "*.{mts,cts,ts,tsx,yml,js,jsx,mjs,cjs,json,md,mdx,css}": [
      "npm run format"
    ],
    "package.json": [
      "npm run lint-package"
    ]
  },
  "prettier": {
    "endOfLine": "auto",
    "semi": false,
    "singleQuote": true
  },
  "devDependencies": {
    "@babel/core": "^7.18.2",
    "@babel/preset-env": "^7.18.2",
    "@babel/preset-typescript": "^7.17.12",
    "@types/jest": "^28.1.3",
    "@types/node": "^17.0.25",
    "auto": "^9.50.1",
    "chromatic": "^5.0.0",
    "eslint": "^8.32.0",
    "eslint-plugin-wesjet": "^0.0.34-rc.1",
    "husky": "^8.0.3",
    "jest": "^27.3.1",
    "lint-staged": "^13.2.1",
    "prettier": "^2.8.1",
    "rimraf": "^5.0.0",
    "sort-package-json": "^1.51.0",
    "ts-jest": "^27.0.7",
    "ts-loader": "^7.0.5",
    "ts-node": "^9.1.1",
    "ts-patch": "^2.1.0",
    "tsx": "3.7.1",
    "turbo": "^1.8.5",
    "typescript": "^4.9.4"
  },
  "engines": {
    "node": "^16.13.0 || ^18.12.0"
  }
}
